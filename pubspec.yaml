name: flutter_webrtc_socketio_videocall
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc:
  socket_io_client:
  get:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true